# realistic-al

## Environment

Always source the `.env` file before running anything to set required environment variables (`DATA_ROOT`, `EXPERIMENT_ROOT`, etc.):

```bash
source .env
```

Always run commands using the `real-al` conda environment:

```bash
conda run -n real-al <command>
# or activate first:
conda activate real-al
```
